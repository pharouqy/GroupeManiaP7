/*export default function authHeader() {
  // return authorization header with jwt token

  if (token) {
    return { Authorization: "Bearer " + token }; // for Node.js Express back-end
    //return { 'x-access-token': token };       // for Spring Boot back-end
  } else {
    return {};
  }
}*/
